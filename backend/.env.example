# Database Configuration
# PostgreSQL connection string for Neon Serverless
# Get from Neon console: https://console.neon.tech
# IMPORTANT: Use "postgresql+psycopg://" (not "postgresql://") for psycopg3 driver
DATABASE_URL=postgresql+psycopg://user:password@your-neon-endpoint.neon.tech/dbname?sslmode=require

# Authentication Configuration
# Shared secret for JWT token signing and verification
# CRITICAL: Must be MINIMUM 32 characters and IDENTICAL on frontend and backend
# Generate secure secret with: openssl rand -base64 48
BETTER_AUTH_SECRET=your_secret_key_minimum_32_characters_long_replace_me

# SQLAlchemy Configuration
# Set to true to log SQL queries (useful for debugging)
SQLALCHEMY_ECHO=false

# OpenRouter Configuration (for Agent Service)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Agent Model Configuration
# Free models: google/gemini-2.0-flash-exp:free, meta-llama/llama-3.3-70b-instruct:free
# See available models: https://openrouter.ai/models
AGENT_MODEL=google/gemini-2.0-flash-exp:free

# MCP Tool Endpoint (backend URL for tool invocation)
MCP_TOOL_ENDPOINT=http://localhost:8000

# Default User ID (for development/testing)
USER_ID=default-user
